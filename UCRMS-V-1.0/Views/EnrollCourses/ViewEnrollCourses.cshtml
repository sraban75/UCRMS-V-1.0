@model IEnumerable<UCRMS_V_1._0.Models.MyModels.EnrollCourse>

@{
    ViewBag.Title = "ViewEnrollCourses";
}

<h2>All EnrollCourse's List</h2>

<p>
    @Html.ActionLink("New Enroll", "Enroll")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.Student.Name)
        </th>
        <th>
            Course-Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.Student.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EnrollCourseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EnrollCourseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EnrollCourseId })
        </td>*@
    </tr>
}

</table>
